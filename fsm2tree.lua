#!/usr/bin/lua

require("gv")

local pairs, ipairs, print, table, type, assert, gv, io
   = pairs, ipairs, print, table, type, assert, gv, io

module("fsm2tree")








function fsm2tree(fsm, outfile)

   

end
